# SillyTavern Leslie 当前状态与后续计划

> 审查日期：2026-08-10
> 当前分支：`codex/modern-chat-ui`
> 本次整理前的基线提交：`d03a82cc045b1096a2b4e829195e14491d70ca11`
> 本文件记录“代码现在实际做到哪里”；长期目标、数据路径和不可破坏的原则仍以 `PROJECT_BRIEF.md` 为准。

## 1. 一句话结论

项目已经进入“中期可运行原型”阶段：SillyTavern 原有聊天内核仍然保留，Electron 桌面外壳、现代聊天界面、Leslie 记忆第一版、Persona / 剧情线身份隔离基础、朋友圈发布与时间线，以及火山引擎角色语音已有实际代码。2026-08-10 已把原开发目录和 Windows 便携目录无损合并到唯一 Git 工作目录；日常聊天、继续开发和生成无私人数据的分发包现在共用同一源码基线。关键改动与合并工具已整理为本地 Git 快照，朋友圈 AI 互动、项目系统和局域网同步尚未开始，因此还不适合作为正式发布版。

当前最重要的工作不是继续增加大功能，而是先收口现有原型：保护数据、修完回归测试、整理并提交代码、补齐兼容性与安全验证。

## 2. 当前架构

```text
桌面快捷方式
    └─ Electron 桌面窗口
        └─ 项目内 SillyTavern 服务器（../../server.js）
            ├─ 原有聊天、角色卡、群聊、World Info、模型适配与 JSONL 数据
            ├─ Leslie 身份接口（/api/leslie/identity）
            ├─ Leslie 记忆接口（/api/leslie/memory）
            ├─ Leslie 朋友圈接口（/api/leslie/moments）
            └─ 火山引擎语音代理（/api/volcengine/generate-voice、/api/volcengine/voices）

浏览器界面
    ├─ 原有 SillyTavern 页面和事件系统
    ├─ Leslie 现代聊天布局、设置页和连接状态适配层
    ├─ Leslie 火山引擎角色语音设置页
    ├─ Leslie 记忆扩展
    │   └─ 通过 SillyTavern 标准扩展提示词入口接入，不重写回复生成内核
    └─ Leslie 朋友圈扩展
        └─ 独立全屏页面、发布器、可见范围和本机时间线
```

这套结构总体符合既定方向：Electron 只做桌面外壳，简单界面主要包裹和重新组织原功能，记忆作为隔离模块接入，正式角色和聊天继续使用 SillyTavern 原有数据目录。

## 3. 已实现内容

| 模块 | 当前实现 | 状态 |
| --- | --- | --- |
| 上游基线 | 已引入 SillyTavern `release` 快照 `8172dcd0...` | 已完成 |
| Electron 基础外壳 | 可启动项目服务器和桌面窗口；已修正为使用项目数据，而不是全局测试数据 | 开发版可用 |
| 桌面启动器 | `F:\matth\desktop\Leslie 启动器.lnk` 可启动和关闭项目，固定传入配置与数据路径 | 本机可用 |
| 现代聊天界面 | 已有桌面双栏、移动端单栏、最近聊天、更多菜单和主题样式 | 原型基本完成 |
| 简化设置页 | 已有主从式设置界面、连接类型和本地 API 等页面 | 原型基本完成 |
| 连接状态 | Leslie 已区分正在检测、已验证连接、已配置未验证、未配置四种状态；无效地址和跳过检查不再误报成功 | 已接入并回归 |
| 角色/聊天选择 | 可以按角色和指定聊天文件打开，修复了只选角色时无法恢复目标聊天的问题 | 已接入 |
| Leslie 记忆后端 | 已有数据结构、评分、事件记录、状态与历史存储、HTTP 接口 | 第一版完成 |
| Persona / 剧情线身份 | 已把现实账号、当前 Persona、对话角色/群组和独立剧情线分开建模；使用稳定 UUID 与历史快照 | 基础版完成 |
| Leslie 记忆前端 | 已有单聊/群聊身份、提示词注入、自动分析、编辑/删除/Swipe 失效处理和确认界面；记忆现按 Persona 与剧情线隔离 | 第一版完成 |
| Leslie 朋友圈 | 已有左栏及聊天更多菜单入口、三种动态模式、全部/指定角色可见、本机时间线、筛选、编辑、撤回和恢复；AI 点赞评论与角色主动发布尚未接入 | 发布与时间线第一版完成 |
| AI 辅助角色创建 | “创建角色”已接入独立 AI 角色工坊；创作简报、按需知识核对、单卡写作、独立审校和头像提示词全部复用当前聊天模型连接，不再要求第二套搜索 API；只提供手动图片提示词，不调用图片生成；另可粘贴 Clarix 公益角色网站导出的 V2 JSON，经兼容性与质量检查后带入原编辑器 | 第一版可测试 |
| 自动化测试 | 14 个 Jest 套件、382 项单元测试通过；朋友圈 3 项和原有相关功能 9 项重点浏览器测试通过；另有 2 项旧用例依赖未放入隔离档案的模型配置/历史聊天夹具 | 本轮通过，夹具待补 |
| Windows 桌面交付 | 已生成 Windows x64 懒人便携版，内置 Electron/Node/Chromium、生产依赖、独立数据目录及启动/关闭/日志/备份脚本；仍没有安装器、签名和自动更新 | 便携版第一版完成 |
| 项目系统 | 尚未实现面向用户的“项目”目录和管理界面 | 未开始 |
| 本地账号与局域网同步 | 尚未实现设备发现、配对、冲突处理和同步日志 | 未开始 |

## 4. 关键代码分层

### 4.1 尽量保持不动的 SillyTavern 核心

- 原有消息生成、角色卡、World Info、群聊、Swipe、模型适配和 JSONL 聊天记录仍由上游代码负责。
- 当前对核心文件的直接修改较少：`public/script.js` 只扩展了按指定聊天文件选中角色的能力；`src/server-startup.js` 只挂载 Leslie 身份、记忆与朋友圈接口。
- `webpack.config.js` 的哈希算法调整用于 Electron/Node 兼容，不改变聊天数据或提示词。

### 4.2 Leslie 界面适配层

- 样式：`public/css/leslie-modern.css`、`leslie-chat-layout.css`、`leslie-settings.css`。
- 行为：`public/scripts/leslie-chat-layout.js`、`leslie-settings.js`、`leslie-chat-selection.js`、`leslie-connection-state.js`。
- 做法以包装原页面、监听原事件和镜像原控件为主，因此高级模式和上游能力仍可保留。

### 4.3 Leslie 记忆模块

- 服务端：`src/leslie-memory/`，负责结构校验、评分、事件日志和磁盘存储。
- 身份服务：`src/leslie-identity/`，负责现实账号、Persona、角色/群组和剧情线的稳定编号、别名及历史快照。
- 浏览器端：`public/scripts/extensions/leslie-memory/`，负责识别当前单聊/群聊、显示管理界面、分析消息并注入选定记忆。
- 正式数据位置：`D:\Projects\sillytavern-leslie\data\default-user\leslie\memory`。
- 身份注册表位置：对应用户数据根目录下的 `leslie\identity\registry.json`；记忆数据格式为版本 2，身份注册表格式为版本 1。写入采用临时文件替换，并保留关键变更前的历史快照。
- 旧版记忆不会被自动猜测成某个 Persona；它会保持“未绑定”状态，继续可读，并由用户选择“绑定到当前 Persona”或“为当前 Persona 新建独立档案”。

### 4.4 Leslie 朋友圈模块

- 服务端：`src/leslie-moments/`，负责格式校验、可见范围、剧情线约束、版本历史、原子写入及可恢复的撤回状态。
- 浏览器端：`public/scripts/extensions/leslie-moments/`，以独立扩展提供左栏入口、聊天更多菜单入口、全屏时间线、发布器和人工控制。
- 数据位置：对应用户数据根目录下的 `leslie\moments\timeline.json`，历史版本位于 `leslie\moments\history`；仅打开页面不会创建时间线文件，第一次发布才写入。
- 现实分享记录“当前 Persona 公开分享了什么”，不会硬塞进剧情发生顺序；剧情内动态必须绑定当前 Persona 与当前角色/群组组成的唯一剧情线；轻松调侃与现实、剧情记忆语义隔离。发布后模式不可修改，避免事后改变记忆含义。
- 当前没有伪造点赞、评论或角色动态，界面会明确显示这部分尚待下一阶段接入。

## 5. 本次验证结果

### 5.1 已通过

- 完整单元回归：14 个测试套件、382 项测试全部通过。
- 主项目完整 ESLint 代码检查通过；本轮新增和修改的测试文件 ESLint 零错误零警告。测试目录全量检查只有一项上游旧 Playwright 条件分支警告，没有错误。
- 朋友圈浏览器回归：桌面发布、指定角色可见、编辑、撤回与恢复，剧情内动态锁定当前剧情线，以及 390×844 手机全屏页面和角色选择器共 3 项通过。
- 原有相关功能浏览器回归：记忆桌面工作流、后端过期提示、手机全屏记忆页共 3 项通过；手机/桌面聊天导航 2 项通过；桌面、手机和中文设置页 3 项通过；群聊记忆帮助模块 1 项通过，共 9 项通过。
- 浏览器测试使用 `test-results` 下的独立临时数据根目录，日志确认没有指向正式项目 `data` 或便携版 `UserData`。

### 5.2 仍需补齐的测试夹具

1. API 状态旧用例需要一份已选择聊天补全供应商的配置；全新隔离档案仍是默认连接类型，页面不会生成该供应商专属的思考开关。
2. “恢复最近真实聊天”旧用例需要至少一个已有历史文件且当前指针为临时值的角色；全新隔离档案只有示例角色，没有这种历史状态。

这两项是测试前置数据缺失，不是本轮身份或记忆逻辑报错。原先记录的手机记忆入口、面板圆角和中文标题定位三项已按真实用户流程或稳定定位收口；其中中文设置页已单独复跑 3 项全部通过。

### 5.3 AI 角色工坊增量验证（2026-08-05）

- 角色工坊核心单元测试 12 项全部通过，相关文件语法检查与针对性 ESLint 检查通过；覆盖模型 JSON 提取、CCV3 归一化、知识核对、质量门槛、头像提示词、对话角色标签兼容、原创建表单映射、四阶段写作规则、Clarix V2 转换与扩展字段保留，以及批量/高级字段防丢失拦截。
- 使用正式项目服务器和无界面 Edge 完成桌面、390px 手机、深色与浅色主题验收。浅色主题中被旧主题全局 `small` 样式覆盖的浅字、占位文字和自定义浅蓝强调色已修复；浏览器无运行时异常和相关控制台错误。
- “粘贴网站 JSON → 本地检查 → 带入角色编辑器”已实际跑通；Clarix 个性化扩展 `extensions.clarix_personalization` 得以保留。含嵌入式世界书、V3 附件等轻量入口无法无损处理的 JSON 仍会明确拒绝并提示改走原生导入。
- 已用当前配置的聊天模型多轮执行真实内存生成测试。最终测试角色“林雾遥”综合质量分 97，本地硬规则 100，模型复审 94；首条消息含可接话台词，6 组示例均使用可移植的 `{{user}}:` / `{{char}}:`，初识对话未出现廉价亲密套话。
- 最终真实生成只发出 3 次 `/api/backends/chat-completions/generate` 请求；搜索 API 0 次、图片 API 0 次。头像区域只返回可复制的英文正向/负向提示词和 2:3 建议，不生成或保存图片。
- 测试中发现并修复了：页面启动图片元数据请求造成的监控误报、“不替玩家”被本地规则误判、示例标签被 SillyTavern 宏提前替换成当前账号名、首条只有动作无台词，以及初识阶段“缘分/等你/比某物重要”等过早亲密套话。
- 正式角色、聊天、记忆均未写入；所有真实生成结果只存在于测试浏览器内存，最终报告写在 `test-results/leslie-character-workshop-live-report.json`。
- 验证截图包括 `test-results/leslie-character-workshop-light.png`、`leslie-character-workshop-mobile.png`、`leslie-character-workshop-json-import.png` 和 `leslie-character-workshop-live-generation.png`。

### 5.4 原作复刻与联网能力实测（2026-08-05）

- 以《STEINS;GATE 0》比屋定真帆为对象，用官方角色页、官方故事页和官方小说试读建立事实基准。未提供事实答案的首轮虽然显示 93 分，但把 21 岁写成约 30 岁、把红莉栖的大学前辈写成恩师/学生关系，并误置讲座地点；手工原作事实评分仅 4/10，证明原有分数不能代表原作真实性。
- 使用同一聊天 API、同一写作流程，只补入官方事实包后，21 岁、大学前辈、研究机构、人工智能/记忆研究、莱斯基宁助手和 ATF 来日背景均得到纠正；手工原作事实评分约 8/10。剩余问题主要是把合理推断写入主要描述，以及示例回复仍偏长。
- 两轮请求都只有 4 次 `/api/backends/chat-completions/generate`，搜索 API 0 次、图片 API 0 次；当前模型也明确承认无法实时联网。因此现状是“模型记忆核对”，不是真正联网搜索。外部官方事实包能显著提升结果，后续应优先接入可验证来源，而不是继续增加角色数量。
- 已修复测试发现的两项确定性问题：正常工作条件中的“只要你”不再被误判为过早亲密；`{{user}}` / `{{char}}` 在生成前会被保护，避免展开成当前账号名，成卡时再恢复为可移植宏。复刻模式的 UI 分数已改称“结构与行为质量分”，并注明未实时核验的原作事实不计入该分数。
- 12 项角色工坊单元测试与针对性 ESLint 检查通过；最终正式服务器浏览器回归通过，桌面/手机、深浅主题、JSON 导入、头像提示词和 4 次聊天请求均正常，无浏览器运行时异常。正式角色、聊天和记忆仍未写入。
- 完整对照记录见 `test-results/leslie-character-workshop-adaptation-benchmark.md`，最终报告见 `test-results/leslie-character-workshop-adaptation-grounded-fixed-live-report.json`。

### 5.5 Windows x64 懒人便携版（2026-08-05）

- 成品位于 `F:\AI\Leslietavern`，约 682.3 MB。`Runtime` 内置 Electron 41.1.1、Node.js 24.14.0 和 Chromium；`App` 只保留桌面运行源码与生产依赖，开发依赖已经裁掉。目标电脑不需要安装 Node.js、npm 或额外浏览器环境。
- 便携版使用独立 `UserData`、`Config`、`Cache`、`Logs`、`Backups` 和 `Run`。配置只监听 `127.0.0.1:8127`，不自动启动外部浏览器；Electron 支持单实例，并可把自身缓存固定在包内目录。
- 根目录提供“启动 LeslieTavern.cmd”“关闭 LeslieTavern.cmd”“备份用户数据.cmd”“查看运行日志.cmd”和“打开用户数据目录.cmd”。关闭脚本只处理本包记录的 PID；备份脚本要求先退出程序，再把 `UserData` 压缩到 `Backups`。
- 初次交付时没有复制正式 `data`、角色、聊天、API 密钥、记忆或项目备份，`UserData` 为空。用户随后已在便携版内自行配置并测试，因此当前 `F:\AI\Leslietavern\UserData` 属于用户数据，后续更新只覆盖 `App` 程序文件，不得清理或替换该目录。
- 成品真实启动通过，日志确认 `Using data root: F:\AI\Leslietavern\UserData`，HTTP 首页返回 200，重复启动被拦截。成品服务器的桌面/手机安全布局、深浅主题、角色工坊和 Clarix JSON 无保存冒烟测试全部通过，无浏览器运行时异常。
- 首轮测试发现精简时误删了 `default/config.yaml` 与 `webpack.config.js`；已确认二者属于首次配置补全和前端资源服务的运行时文件，补回后重测通过。启动、关闭和备份脚本均已实际执行；最终 8 个核心文件的 SHA-256 全部匹配。
- 本版没有局域网/三端同步模块；未删除与桌面页面共用的响应式 UI 代码，以免破坏窗口缩放。完整报告见 `test-results/leslietavern-windows-portable-report.md`。

### 5.6 API 初始化状态与思考模式修复（2026-08-05）

- 找到初始化状态误判根因：SillyTavern 的同一状态字段既可能是模型名/“Valid”，也可能是“地址无效”“仅保存密钥、尚未验证”“跳过状态检查”；旧 Leslie 适配只排除了 `no_connection`，会把其余错误或未验证文字当成已连接。现已加入无依赖的状态分类层，并监听设置加载、连接配置加载和加载动画，界面明确区分“正在检测 / 已连接 / 已配置 / 未连接”。
- 找到同一 `deepseek-v4-flash` 在正式项目和便携版表现不同的原因：正式项目保存的是 `show_thoughts: false`，便携版新用户沿用了上游默认 `true`；模型和接口并没有因为打包路径而变化。Leslie 模型页现提供“思考模式（默认关闭）”开关，复用原始 SillyTavern 设置；关闭时同时移除显式推理强度。DeepSeek 请求层会据此发送 `thinking: disabled`，不是仅隐藏思考文字。
- 新用户默认已改为不请求思考；现有便携用户通过应用自身保存流程更新为 `show_thoughts: false`、`reasoning_effort: auto`。没有改动 API 地址、模型名、密钥、角色、聊天或提示词顺序。
- 新增 5 项连接状态单元测试，全部通过；全部 Leslie 单元回归为 4 套件、29 项通过，相关主代码与测试代码 ESLint 零错误零警告。便携版 Playwright 失败连接场景通过，真实已配置 API 成功连接场景也通过；重启后 UI 实测为“已连接”，两个思考开关均为关闭。
- 便携版仅更新 7 个 `App` 内程序文件，哈希与工作区源码一致，`UserData` 未被构建脚本覆盖。安全重启后 PID 正常、HTTP 200，日志再次确认 `Using data root: F:\AI\Leslietavern\UserData`。界面截图见 `tests/test-results/leslie-api-ux.png`。

### 5.7 思考模式截断保护与真实验收（2026-08-05）

- 找到回复风格设置不生效的根因：Leslie 回复页一直镜像 `amount_gen / max_context`，而 DeepSeek 聊天补全实际读取 `openai_max_tokens / openai_max_context`。四种风格现已按当前 API 类型映射到正确控件；原提示词、角色卡和聊天格式未改。
- DeepSeek 的 `max_tokens` 由思考过程和最终正文共享。开启思考时，新增请求期安全预算：普通/自动/高强度至少为思考与正文预留 32K 输出、32K 输入，最大思考预留 64K 输出、64K 输入；只改变该次请求和提示词裁剪预算，不覆盖用户保存的长度与上下文数值，关闭思考后仍严格使用用户值。
- 客户端现记录流式 `finish_reason`。服务端明确返回 `length` 时，单聊会复用原生“继续”流程自动补全，最多两次；空回复、用户扮演、群聊、已中止、正在发送或输入框已有新文字时不会擅自续写，达到重试上限会提示用户手动继续，避免无限请求。
- 新增思考预算与长度续写单元测试；两项新功能连同连接状态的针对性回归共 3 套件、21 项通过，完整 Jest 回归为 12 套件、360 项全部通过。全部相关主代码和测试 ESLint 零错误零警告，最终便携版设置界面 Playwright 冒烟通过。
- 使用便携版当前已保存的 DeepSeek V4 Flash 连接进行四种回复风格真实验收，全程开启思考且不保存聊天。简洁、对话、均衡、长篇四次请求均使用安全预算并以 `finish_reason: stop` 自然结束；思考和正文均非空，原始及渲染后的中文没有乱码、替换字符、控制字符或思考标签泄漏。首次命中异常是测试提示自身包含标签造成的污染，移除后复测通过。
- 最终便携版更新 5 个 `App` 程序文件，SHA-256 与工作区一致；安全重启后 HTTP 200，日志确认数据根目录仍为 `F:\AI\Leslietavern\UserData`。验收结束已通过应用原保存流程恢复用户原设置：思考关闭、推理强度自动、回复上限 300、上下文 4095；API 地址、密钥、模型、角色和聊天未改。

### 5.8 Persona / 剧情线身份隔离第一版（2026-08-05）

- 记忆不再把“登录账号背后的现实用户”和“聊天里正在扮演的人”混为一个身份。系统现分别记录现实账号所有者、当前 Persona、对面的角色/群组，以及由两者组成的独立剧情线；模型提示只把当前 Persona 当作用户消息的剧情身份，不把现实账号所有者写进角色记忆。
- 新增每用户独立的稳定身份注册表。Persona、角色、群组和剧情线使用 UUID；改名或换头像只有在用户明确确认仍是同一个人时才沿用旧编号，同时保留旧来源键、旧名称和注册表历史快照。
- Leslie 记忆格式升级到版本 2，并在清单中保存 Persona 与剧情线绑定。服务端在新增/修改记忆、恢复状态、同步角色核心、分支和读取提示上下文时校验剧情线编号；编号不一致会拒绝操作，避免前端偶然选错档案后继续写入。
- 聊天元数据升级为可同时保存多个 Persona 的记忆档案。切换 Persona 时，旧档案不会继续注入提示词；旧版本未绑定档案不会自动迁移，界面会明确提供“绑定到当前 Persona”“为当前 Persona 新建空档案”和“确认改名/换头像后仍为同一 Persona”三种人工处理入口。
- 完整 Jest 回归为 13 套件、371 项全部通过；主项目完整 ESLint 通过，本轮测试文件 ESLint 零错误零警告。身份/记忆、手机/桌面导航、设置和群聊帮助共 9 项相关 Edge 浏览器测试通过。
- 已把 10 个程序文件增量部署到 `F:\AI\Leslietavern\App`，旧文件备份在 `F:\AI\Leslietavern\Backups\App-identity-foundation-20260805-221619`。复制前后便携版 `UserData` 指纹完全一致；启动验证后 HTTP 200、标准错误日志为空，并再次确认 `Using data root: F:\AI\Leslietavern\UserData`。启动本身按 SillyTavern 原有机制更新了 `content.log`、`QuickReplies\Default.json` 和 `settings.json`，没有创建、删除或迁移角色、聊天及记忆档案。
- 本轮只建立朋友圈所需的身份与记忆安全地基，尚未实现朋友圈页面、动态数据、可见角色选择、点赞评论或角色主动发动态。

### 5.9 朋友圈发布与时间线第一版（2026-08-05）

- 新增可直接使用的朋友圈入口：桌面左侧边栏有明显的“朋友圈”按钮，聊天页“更多”菜单也可进入；手机上使用全屏页面，关闭后回到原聊天界面。
- 用户可以按当前 Persona 发布纯文字动态，选择现实分享、剧情内动态或轻松调侃。现实分享可以设为所有角色可见或勾选指定角色；剧情内动态只有进入具体单聊/群聊后才能选择，并强制绑定当前剧情线；编辑时不允许更换模式。
- 时间线支持按模式筛选、显示已撤回、编辑本人动态、二次确认撤回和恢复。所有数据保存在当前用户本机目录；写入使用临时文件替换，每次改动前保留历史版本，损坏文件会停止并报错，不会静默覆盖。
- 身份接口新增批量解析，指定角色名单一次写入稳定身份注册表；服务端再次校验发布 Persona、可见角色和剧情线，不能只靠前端传入文字名称。
- 相关后端、API、浏览器模型共新增 10 项测试，身份批量解析新增 1 项；完整 Jest 为 14 套件、382 项全通过。主项目 ESLint 全通过，测试目录仅保留一项与本轮无关的旧警告；朋友圈 3 项及原有聊天/记忆/设置 9 项 Edge 实机回归全部通过。
- 桌面和手机验收截图位于 `test-results/leslie-moments-desktop.png` 与 `test-results/leslie-moments-mobile.png`。本阶段不调用聊天模型、不改变提示词顺序，也不把动态自动写进 ABC 记忆；AI 点赞、评论、角色主动发动态及其人工审核开关留给下一阶段。
- 已将 10 个程序文件增量部署到 `F:\AI\Leslietavern\App`，其中 3 个被替换的旧文件已备份到 `F:\AI\Leslietavern\Backups\App-moments-ui-20260805-234941`，7 个朋友圈文件为新增；工作区与便携版 10 个文件的 SHA-256 全部一致。便携版启动后首页和朋友圈清单均返回 HTTP 200，日志确认固定数据根目录仍为 `F:\AI\Leslietavern\UserData`，并列出 `leslie-moments` 扩展；验收后已安全退出。启动过程只按原机制更新了两个 `content.log`、`QuickReplies\Default.json` 和 `settings.json`，没有新增或改动角色、聊天、记忆和朋友圈时间线。

### 5.10 火山引擎角色语音第一版（2026-08-07）

- 复用 SillyTavern 原有 TTS 队列、播放按钮、群聊角色识别和按角色保存音色映射，没有另建第二套聊天或播放器，也没有改变回复文字、提示词顺序、角色卡和 JSONL 聊天格式。
- Leslie 简单设置新增独立的“角色语音”页面：可保存火山引擎 App ID、Access Key 和 Resource ID，调整语速，选择朗读完整回复或只读引号内对话，为当前单聊/群聊角色分配内置或自定义 Speaker ID，并用中文短句试听。只是打开页面不会切换现有供应商；用户明确启用后才同步选择原 TTS 的 `Volcengine` 供应商。
- 自动朗读改为等待回复短暂稳定 450 毫秒后，重新读取当前消息的完整文字。新一轮生成、自动续写、Swipe、删除消息和切换聊天会取消过期任务；生成结束还会补查一次最终消息，避免续写路径只朗读前半段或重复朗读。日志只记录字符数，不再输出聊天正文。
- 服务端只允许火山引擎官方 V3 HTTPS 地址，补齐必填参数和长度校验、语速范围收敛、45 秒超时、上游错误处理、空音频检查、`no-store` 响应和安全错误提示，避免带密钥的接口被当作任意网址代理。App ID 与 Access Key 继续使用 SillyTavern 原密钥库保存，不进入浏览器设置对象和聊天记录。
- 新增 6 项语音安全边界与稳定调度单元测试；完整 Jest 回归为 16 套件、388 项全部通过。本轮主代码与测试文件 ESLint 零错误，相关文件 Node 语法检查通过。桌面与 390×844 手机设置页共 2 项 Edge 隔离浏览器测试通过，测试实际启用并同步了原 TTS 控件，且拦截确认未发出真实语音 API 请求。
- 工作区浏览器测试使用 `test-results` 下的新临时数据根目录；便携版部署后又从 `F:\AI\Leslietavern\App` 启动仅绑定 `127.0.0.1:8138` 的独立验证实例，数据位于 `Run` 下的临时目录。两次测试都未指向正式 `data` 或便携版 `UserData`，验收后均已停止服务器并删除临时目录。
- 已向 `F:\AI\Leslietavern\App` 增量部署 10 个程序文件，其中 6 个旧文件完整备份、4 个为新增文件；备份与逐文件新旧 SHA-256 清单位于 `F:\AI\Leslietavern\Backups\App-volcengine-tts-20260807-140230`。部署前、复制后和便携版独立验证后，正式 `UserData` 均为 242 个文件、34,972,377 字节，整树内容指纹保持 `6541BCB70725F992DDBB7066F5C175989A0611A2C95A2BB57AEE089A472BBD6B`；角色、聊天、密钥、记忆和朋友圈数据均未被部署或测试流程修改。最终便携版 10 个文件与工作区源文件哈希全部一致，便携版保持未启动状态。
- 尚待用户在火山控制台提供与所购服务匹配的真实 App ID、Access Key、Resource ID 和可用 Speaker ID 后做一次真实试听与长回复验收。第一版不把音频保存到磁盘、不做跨会话缓存；角色音色沿用上游以角色显示名为键的格式，角色改名后可能需要重新选择一次音色。

### 5.11 火山引擎全量音色与多语种修复（2026-08-07）

- 修复 Leslie 简单设置保存 `voice_type`、而原 TTS 供应商只按显示名称查找导致内置音色无法播放的问题。供应商现在同时兼容显示名称与原始 `voice_type`，旧设置和新设置不需要迁移。
- 新增只读取固定火山引擎公开文档地址的服务端音色目录接口；当前官方文档可解析 586 个普通语音合成音色，其中 TTS 2.0 为 443 个、TTS 1.0 为 143 个，实时语音大模型的 25 个音色因接口不同被明确排除。前端再保留 5 个未出现在当前文档中的旧版兼容音色，总计显示 591 个可用音色，并支持按名称、`voice_type`、语种、分类和模型搜索。官方音色会自动使用文档所属模型的 Resource ID，自定义/复刻音色继续使用用户填写的备用 Resource ID。
- 增加“自动识别、跨语种、中文、英文、日文”语言模式。默认自动模式会根据每条回复选择 `zh-cn`、`en`、`ja` 或 `crosslingual`，同时保留火山 V3 的语言检测，并把不支持字符比例阈值放宽到官方允许的 1，解决中文音色遇到英文回复直接失败的问题。
- 自定义音色输入现在接受纯 ID、带引号 ID、JSON、`voice_type=...`、`speaker_id: ...` 等常见控制台复制格式；成功导入后会立即加入列表并设为默认音色，重复导入也会直接选中，不再表现为“无法填入”。
- 新增目录解析、Resource ID 分类、语言选择和自定义 ID 导入测试；本轮 2 个相关 Jest 套件 13 项全部通过，6 个主代码文件 ESLint 零错误。完整回归 17 套件中 16 套件通过、392 项通过；仅原有 `private-request-filter.test.js` 在当前 Node 24 / Jest 模拟环境下仍有 5 项失败，与本轮语音文件无关。
- 实时读取火山官方文档返回 HTTP 200，更新时间为 `2026-08-03T08:12:54Z`，实际解析数量和模型分类符合预期。已部署便携版的浏览器验收确认首页有内容、无错误覆盖层、无页面错误，`/api/volcengine/voices` 返回 586 项；角色语音页显示 591 个可用音色、自动语言、搜索框和自定义输入，两个角色下拉框清空搜索后共恢复 1184 个选项。
- 已把 7 个程序文件增量部署到 `F:\AI\Leslietavern\App`，6 个旧文件备份到 `F:\AI\Leslietavern\Backups\App-volcengine-tts-catalog-20260807-151304`，另新增 1 个服务端目录解析模块；7 个目标文件与工作区 SHA-256 全部一致。复制程序文件前后，正式 `UserData` 均为 253 个文件、35,201,327 字节，整树指纹保持 `B28358A2B0D92A43D77DFFCCD5304BE3E71F775A66FCF7BF53608A7B40B20061`。
- 按用户要求，通过 SillyTavern 正式角色接口在便携版角色库新增“小铃·中文语音测试”：18 岁、娇小、活泼温柔，只使用简体中文并优先回复一到三句话；没有上传自定义头像，角色卡使用系统中性占位图。正式角色文件为 `UserData\default-user\characters\xiaoling-chinese-voice-test.png`，角色 API 和角色管理界面都已确认可见。创建角色与浏览器验收后，`UserData` 为 259 个文件、35,312,436 字节；新增内容仅为角色卡、角色/Persona 缩略图和角色缓存，正常启动还更新了两个 `content.log`、`QuickReplies\Default.json`、`settings.json` 与 `stats.json`。最终便携版和隐藏验证服务器均已停止。

### 5.12 火山引擎真实链路诊断与播放器加固（2026-08-07）

- 使用便携版正式 `UserData` 中已保存的凭证，通过正在运行的 Leslie 服务端 `/api/volcengine/generate-voice` 做了真实计费链路测试；没有读取或输出密钥值。小铃音色 `ICL_uranus_zh_female_jiaoruoluoli_tob` 配合 `seed-tts-2.0` 的中文、纯英文、中英混合和强制跨语种请求均返回 HTTP 200，分别得到 21,357、38,829、47,085 和 38,829 字节 MP3。角色聊天中两条真实小铃回复原文也分别返回 70,893 与 69,357 字节 MP3；此前保存的 `test-results\xiaoling-real-chain.mp3` 为 34,221 字节且具有有效 ID3/MP3 文件头。
- 日志中曾出现两次火山上游 HTTP 403，但旧默认音色和小铃音色随后使用相同凭证、Resource ID 与请求格式都可稳定返回 200，说明凭证、语言识别、音色授权和当前后端主链路已经可用。旧代码没有保留火山的 `X-Api-Status-Code`、`X-Api-Message` 或错误正文，无法再还原那两次 403 的具体原因。
- 已补充安全的上游错误诊断：只记录/返回截断后的错误码、错误说明、音色、Resource ID 和火山 Log ID，不记录凭证或朗读正文。离线备用音色现在各自固定携带 `seed-tts-2.0`，避免官方目录暂时不可用时误用用户为复刻音色填写的备用 Resource ID。
- 已加固原 TTS 播放队列：显式等待 `audio.play()`，播放被浏览器或音频设备拒绝时给出中文提示并释放队列；清理重复 `ended` 监听，阻止取消后的旧音频重新播放；Electron 窗口显式使用 `no-user-gesture-required` 自动播放策略。5 个修改文件已通过 Node 语法检查，语言判断、错误诊断、自定义 `voice_type=...` 导入和备用 Resource ID 的内联断言通过。
- 经用户在了解停止、备份、覆盖和重启范围后明确授权，已把 5 个程序文件增量部署到 `F:\AI\Leslietavern\App`；5 个旧文件完整备份到 `F:\AI\Leslietavern\Backups\App-volcengine-tts-playback-20260807-170053`，工作区与便携版 5 个文件的 SHA-256 全部一致。新进程 PID 为 46152，启动日志继续确认 `Using data root: F:\AI\Leslietavern\UserData`，没有复制、删除或手工编辑 `UserData`。
- 重启后通过正式 Leslie 服务端再次实测小铃中文、英文、中英混合和角色实际回复，均返回 HTTP 200 与 `audio/mpeg`，大小分别为 35,565、38,829、47,085 和 69,357 字节；火山 Log ID 均已取得，官方目录接口返回 586 个音色，Leslie 页面合并兼容音色后显示 591 个可用音色。
- 隔离 Edge 实机验收确认角色语音和回复后自动朗读均已开启、语言为自动识别，小铃映射为“娇弱萝莉 2.0”。点击小铃试听后浏览器请求返回 200，页面显示“试听语音已开始播放”；随后用已有最后一条小铃回复触发原 TTS 播放队列，依次观察到任务开始、MP3 入队、音频就绪、开始播放，播放按钮由停止状态自然恢复为空闲，最终请求为 200 且无页面错误。验收浏览器已关闭，便携版保持运行。

### 5.13 新回复自动朗读触发修复（2026-08-07）

- 用户实测确认手动播放正常但角色新回复不会自动播放。隔离 Edge 开启事件追踪并向小铃发送真实消息后复现：回复生成、`GENERATION_ENDED` 与 `CHARACTER_MESSAGE_RENDERED` 都正常发生，但没有火山请求；控制台明确报 `TypeError: Illegal invocation`，错误发生在 `SettledTtsScheduler.schedule()` 调用浏览器原生 `setTimeout` 时。
- 根因是调度器把浏览器原生 `setTimeout` / `clearTimeout` 保存为类的私有方法后直接调用，导致 Chromium 将调度器实例当作原生计时器的接收对象；浏览器要求接收对象为全局 `Window`，因此自动链路在等待 450 毫秒这一步中断。手动播放不经过该调度器，所以此前会表现为“手动能播、自动不播”。
- 修复只改动 `public/scripts/leslie-tts-stability.js`：通过 `Reflect.apply` 显式用 `globalThis` 调用计时器，并增加“计时器要求全局接收对象”的回归测试。文件通过 Node 语法检查与浏览器绑定内联断言；没有修改消息生成、提示词顺序、角色卡、聊天格式或火山请求格式。
- 部署前已把旧调度器备份到 `F:\AI\Leslietavern\Backups\App-volcengine-tts-auto-trigger-20260807-171527`，工作区与便携版新文件 SHA-256 一致。便携版重启为 PID 44980，日志继续确认 `Using data root: F:\AI\Leslietavern\UserData`。
- 修复后的关键实机验收只发送了一条聊天消息，没有点击试听或播放按钮：小铃回复“今天心情特别好……”后，控制台自动出现 `Adding settled message`、TTS 任务开始、MP3 入队和 `Starting TTS playback`；浏览器自动请求 `/api/volcengine/generate-voice` 返回 HTTP 200，播放按钮自动切换为停止状态并在自然播放结束后恢复为空闲，页面有内容、无错误覆盖层、无页面错误。
- 为便于恢复，首次故障复现前的完整小铃聊天备份位于 `F:\AI\Leslietavern\UserData\default-user\backups\chat_xiaoling_auto_tts_debug_20260807-171214.jsonl`。本轮实机测试在原聊天中新增两组“自动朗读测试”问答；没有修改其他聊天、角色或密钥。

### 5.14 自动朗读首句延迟优化（2026-08-07）

- 自动朗读的稳定等待由 450 毫秒缩短到 120 毫秒，同时保留续写、重试、Swipe、删除消息和切换聊天时取消旧任务的保护；角色回复文字、提示词、聊天格式与火山请求参数未改。
- 移除三处旧的 1 秒轮询等待：完整消息入队、清洗后的朗读片段入队和 MP3 音频入队后都会立即唤醒原 TTS 处理器；一段播完也会立即推进下一段。仍复用 SillyTavern 原队列与播放器，没有新增第二套播放链路。
- 首轮计时发现“回复完成到火山请求”仍为 959.6 毫秒，并通过队列日志定位到片段转换后的旧轮询；补齐即时推进后，第二轮小铃实测降至 112.8 毫秒。该次火山合成本身为 2091.4 毫秒，回复完成到自动调用播放器共 2225.9 毫秒，音频就绪到调用播放仅 1.2 毫秒。
- 最终验收没有点击试听或播放按钮；小铃回复后 `/api/volcengine/generate-voice` 自动返回 HTTP 200，9.5 秒音频自然播放结束，播放器状态为 `ended: true`、无媒体错误、无页面错误，隔离浏览器已关闭。火山网络与合成耗时仍会随网络和文本长度波动，本地固定等待已压缩到约 0.12 秒。
- 语音调度 4 项 Jest 测试全部通过，相关文件 Node 语法检查和 ESLint 均通过。便携版三个文件首次更新前的备份位于 `F:\AI\Leslietavern\Backups\App-volcengine-tts-latency-20260807-173219`，片段队列补丁前的 `index.js` 备份位于 `F:\AI\Leslietavern\Backups\App-volcengine-tts-latency-stage2-20260807-173645`；目标文件哈希与工作区一致，重启后 HTTP 200，日志继续确认 `Using data root: F:\AI\Leslietavern\UserData`。
- 最终计时前的小铃聊天备份位于 `F:\AI\Leslietavern\UserData\default-user\backups\chat_xiaoling_tts_speed_20260807-173319.jsonl`。两轮速度测试只在小铃原测试聊天中新增两组问答，没有修改其他聊天、角色、密钥或语音配置。

### 5.15 只朗读括号外对话（2026-08-08）

- Leslie 角色语音的“朗读内容”增加第三个互斥模式“只读括号外内容（（）与 ()）”，用于角色卡以圆括号书写动作、括号外直接书写台词的格式。原有“完整回复”和“只读引号内对话”保持不变；完整语音高级设置也同步增加对应开关。
- 新增独立纯文本过滤模块 `public/scripts/leslie-tts-text-filter.js`，支持中文全角、英文半角、多段、嵌套与混合宽度圆括号。过滤只作用于发送给 TTS 的临时文本，不修改聊天消息、角色卡、JSONL、提示词或记忆；未闭合的起始括号从起始处到末尾视为动作说明而不朗读。
- 括号过滤、自动朗读调度和火山请求边界共 3 个 Jest 套件、15 项测试全部通过；相关主文件、单元测试和浏览器用例的 Node 语法检查与 ESLint 全部通过。隔离 Edge 中新增选项可见，选择后简单设置与原 TTS 新开关同时为开启，引号模式和星号动作模式均关闭，页面无错误。
- 浏览器与 Playwright 使用 `test-results\tts-parentheses-20260808-000905\data` 的全新隔离数据根目录，服务器只监听 `127.0.0.1:8141`，验收后浏览器和 PID 43248 均已关闭。角色语音三项旧浏览器回归中，新选项所在主流程和手机布局两项通过；自定义 `voice_type` 自动选中在全新用户夹具中仍有一项既有初始化失败，与括号过滤无关，未使用正式用户设置规避。
- 已把四个程序文件增量同步到 `F:\AI\Leslietavern\App`，三个被替换文件的旧版备份位于 `F:\AI\Leslietavern\Backups\App-volcengine-tts-parentheses-20260808-001333`，新增过滤模块此前不存在；四个目标文件与项目源码的 SHA-256 全部一致。便携版部署前就是关闭状态，部署后仍未启动，因此没有加载正式 `UserData`。
- 项目正式记忆部署前后均为 373 个文件、871,747 字节，整树指纹保持 `7D197D517B587084A79912F3CF1F8784190F2EF86BBC8AF66BA067CB0CCD24F6`；便携版记忆均为 8 个文件、4,308 字节，指纹保持 `6D51B498639B85B2EBA85953269D750FCCDA2CCDED69E02FC9B67D1928261DA3`。两处 `memory-archive` 部署前后均不存在，没有创建、删除、迁移或修改任何记忆文件。

### 5.16 开发目录与便携目录无损合并（2026-08-10）

- 唯一项目根目录调整为 `D:\Projects\sillytavern-leslie`；原 Git 历史、分支和全部未提交源码均留在原位。便携版 `App` 的 953 个运行文件与开发工作区逐文件 SHA-256 相同，开发工作区另有 43 个文档、测试与开发文件，因此没有从旧 `App` 反向覆盖源码。
- 原开发 `data` 有 960 个文件、111,269,772 字节；原便携 `UserData` 有 310 个文件、35,872,787 字节。合并前分别建立完整 ZIP，另建立当前未提交源码 TAR；快照位于 `backups\workspace-merge-20260810`。
- 合并加入便携版独有的 99 个文件，包括“小铃·中文语音测试”角色卡、聊天、两组 Leslie 记忆、朋友圈时间线及历史备份。身份注册表按实体和剧情线合并为 7 个身份、4 条剧情线；便携版独有密钥作为非当前启用的备用项保存；语音映射加入 Leslie、Seraphina 和小铃，同时保留开发数据现有默认语音与朗读模式。
- 同路径的 Seraphina 聊天采用便携版较完整的 12 行版本；开发版原文件仍在合并前完整 ZIP 中。其他同路径冲突以原开发数据为主，便携原件同样保存在快照中。
- 合并后 44 个正式聊天/记忆 JSONL 文件共 1693 行逐行解析通过；角色卡 PNG 文件头全部有效。原开发目录中的 `龙华妃咲`、`丹花伊吹`、`春日野穹`及其聊天均明确保留。
- Electron 41.1.1 运行环境进入根目录 `Runtime`，75 个文件与旧便携运行环境逐一哈希一致。根目录新增中文启动、关闭、备份、数据目录和日志入口；启动器固定使用 `Config\config.yaml` 与唯一正式 `data`，并检查日志中的真实数据根目录。
- Git 明确忽略 `data`、`Runtime`、`Config`、缓存、日志、PID 和备份；便携构建脚本也新增排除统一工作目录运行资产，继续生成空白独立 `UserData`，不会把角色、聊天、密钥或记忆带入分发包。
- 完整 Electron 启动器已实际启动窗口并返回 HTTP 200，数据根目录与本机监听检查通过、标准错误为空；随后由新关闭脚本退出，角色、单聊、群聊和 Leslie 数据指纹保持不变。新备份脚本也已实际生成合并后 ZIP 及 SHA-256。
- 便携构建已在无全局 npm 的环境中通过：`dist\LeslieTavern` 共 21,124 个文件、715,743,207 字节，8 项核心 SHA-256 全部匹配，`UserData` 为 0 个文件；文件名和文本扫描均未发现妃咲、伊吹、穹妹或小铃数据。成品使用隔离测试数据启动返回 HTTP 200，仍只监听本机，测试后成品 `UserData` 保持空白。
- 原便携 `UserData` 已完整保存在 `backups\workspace-merge-20260810\portable-userdata-before.zip`；原便携配置、脚本和清单保存在同目录的 `portable-package-metadata`，历史程序备份与日志进入 `backups\portable-history-before-merge`。为提供最直接的迁移回退，旧便携完整目录另保留为统一根目录下、被 Git 忽略的 `legacy-portable-package`；日常使用、开发和分发构建都不依赖该副本。

## 6. 主要风险

### 6.1 立即处理

1. **尚未配置个人 GitHub 远程仓库。** 当前只有 SillyTavern 官方 `upstream`，本次整理会形成可恢复的本地提交，但在添加属于 LeslieTavern 的 `origin` 前不应推送，避免误把私人分支发往上游项目。
2. **朋友圈 AI 互动尚未接入。** 当前三种模式与身份、剧情线和可见范围已明确保存，但点赞、评论、角色主动发布及其进入 ABC 记忆的审批规则仍需单独实现。下一阶段必须继续保留人工预览、逐条批准、关闭自动写入和撤回恢复能力，不得让模型自行猜测现实用户与当前 Persona 的关系。
3. **局域网功能仍需独立安全设计。** 统一启动器固定使用 `Config\config.yaml`，仅监听 `127.0.0.1:8127`；仓库根部继承上游的开发配置不作为日常入口。正式做局域网同步前必须先设计清楚访问范围、认证和默认安全策略，不能直接放宽当前安全配置。
4. **火山引擎新回复自动播放已通过，长回复取消场景仍待补测。** 正式凭证、小铃音色、中文/英文/混合文本、角色真实回复、前端试听、原 TTS 播放队列和“新回复完成后无人点击自动播放”均已通过实机验收；后续仍建议补一个多段长回复和一次中途 Swipe 取消，确认高延迟与取消状态下不会残留旧音频。

### 6.2 进入下一阶段前处理

1. **前端文件过大。** `leslie-settings.js` 约 1976 行，记忆前端 `index.js` 约 1419 行，三份主要 Leslie CSS 合计超过 5000 行。继续叠加功能会提高修改互相影响的概率，建议按页面、控制器和通用组件逐步拆分。
2. **较依赖页面结构。** 当前界面适配使用多个 DOM 监听器；DOM 是网页元素结构。上游页面改名或换层级时，Leslie 界面可能无报错但局部失效，需要稳定选择器和上游升级回归测试。
3. **记忆存储仍是单机第一版。** 当前采用同步文件读写，事件追加会重写日志，并且还没有并发锁、迁移器、自动压缩和保留期限。单机小规模可用，但不能直接承担多设备同时写入。
4. **缺少“关闭记忆后完全不改变提示词”的金标准测试。** 记忆通过标准扩展入口接入并具备失败放行，但仍应补一组精确对比测试，确保禁用后与原 SillyTavern 输出一致。
5. **Electron 已有便携交付第一版，但还不是正式安装器。** 已补单实例、包内缓存、启动/关闭/日志/备份脚本和 Windows x64 便携目录；仍缺少代码签名、安装/卸载、托盘、自动更新、版本迁移与正式发布流水线。当前便携包已通过无私人数据扫描，适合测试分发，但不应宣称为签名正式版。
6. **角色工坊仍是第一版。** 当前只生成或接收一张内存草稿并带入原编辑器，不自动保存、不生成头像。知识核对复用当前聊天模型；如果模型自身或所接 API 不支持联网，它只能做非实时知识核对并标出不确定项，不能等同于真实网页搜索。原创与一条原作复刻需求已经实测；原作复刻暴露出无来源时会高分输出错误事实的风险，仍需真正接入可验证网页来源，并补不同供应商、超长提示词和失败恢复评测集。

## 7. 推荐后续计划

### 阶段 A：收口当前原型（现在做）

1. 再确认一次正式数据备份和固定路径，继续禁止自动合并全局测试目录。
2. 为 API 状态和最近聊天旧用例补独立、可重复生成的测试夹具，避免继续依赖现有用户数据。
3. 补“记忆关闭时提示词完全不变”、Persona 切换金标准、角色卡/单聊/群聊数据兼容和启动数据根目录检查。
4. 按“数据路径与 Electron、聊天界面、设置页、记忆后端、记忆前端、测试与文档”拆成可审核的小提交。
5. 删除或忽略开发日志等不应入库的临时文件，形成干净且可恢复的版本点。

### 阶段 B：让桌面版可交付

1. 收紧默认网络访问配置；局域网能力默认关闭，启用时有清楚提示和身份验证。
2. 补齐 Windows 安装/便携包、单实例、正常退出、日志查看和数据目录迁移说明。
3. 选最庞大的 Leslie 前端模块先拆分，降低以后改设置页和记忆页的风险。

### 阶段 C：项目系统与记忆加固

1. 先定义“项目”只做组织和引用，不搬动现有角色卡与 JSONL 聊天数据。
2. 为记忆格式加入版本迁移、并发写入保护、压缩和恢复测试。
3. 项目视图稳定后，再考虑项目级记忆聚合。

### 阶段 D：局域网同步（最后做）

1. 先实现本地账号、设备身份、配对和权限。
2. 再做设备发现、增量同步、冲突副本和同步日志。
3. 同步层保持在文件和项目外围，不进入模型回复生成流程。

## 8. 当前发布判断

- 作为本机开发、日常聊天与功能验收原型：统一使用 `D:\Projects\sillytavern-leslie`；根目录启动器会自动核对 `Using data root`。
- 作为长期日常使用版本：应先完成阶段 A，尤其是提交关键代码和修复移动端入口。
- 作为 Windows x64 小范围测试便携版：使用 `packaging\windows-portable\build-portable.ps1` 构建到 `dist`，并保留“未知发布者、无自动更新、首次自行配置 API”的说明。
- 作为可公开发布的签名正式版本：暂不建议；仍需要阶段 A 收口、安装/升级/恢复流程和代码签名。

## 9. 文档职责

- `PROJECT_BRIEF.md`：长期目标、技术原则、用户数据规则和固定路径。
- `PROJECT_STATUS.md`：当前实现、测试结果、风险和下一步；每次阶段收口后更新日期和结论。
- `design.md`：现代聊天界面的设计基线。
- `AGENTS.md`：所有后续开发人员和自动化助手必须遵守的执行规则。
